#include<iostream>
using namespace std;
int main()
{
  int r, a, d;
  cin>>r>>a;
  d = 2*r;
  if(d<=a)
    cout<<"circle can be inside a square";
  else
    cout<<"circle cannot be inside a square";
  //Type your code here.
}
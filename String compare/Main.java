#include<iostream>
#include <bits/stdc++.h>
using namespace std;
int main()
{
  string a, b;
  getline(cin, a);
  getline(cin, b);
  if(a.compare(b)==0)
    cout<<"Strings are same";
  else
    cout<<"Strings are not same";
  //Type your code here.
}